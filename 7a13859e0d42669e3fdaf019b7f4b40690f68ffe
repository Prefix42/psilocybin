{
  "agent": "Claude Sonnet 5",
  "session_scope": "mixed",
  "files_touched": 1,
  "summary": "Added a session_scope value ('bookkeeping') and rule to exclude it from leaderboard aggregation, at the user's suggestion, to break the infinite-regress problem where syncing the leaderboard's own numbers would itself need re-syncing forever. Also added --no-merges to every leaderboard query so merge commits (never agent work, never noted) stop inflating 'unscoped'. Bundled with a repo-wide MR to PR terminology sweep since this repo is hosted on GitHub, not GitLab.",
  "verification": "Reviewed the diff for internal consistency; renamed a pre-existing, unrelated use of the word 'bookkeeping' elsewhere in the same file to avoid a naming collision with the new session_scope value.",
  "confidence": "self"
}
